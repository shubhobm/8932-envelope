function [trmat] = transform(mat)
s = size(mat);
trmat = mat;
fprintf('Start..')
for i = 1:s(1)
    for j = 1:s(2)
        if mat(i,j)<0
            C = -floor(mat(i,j));
        else
            C = 1;
        end
        trmat(i,j) = log(mat(i,j) + C);
    end
end